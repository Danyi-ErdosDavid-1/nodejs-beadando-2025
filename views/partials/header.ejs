<!DOCTYPE html>
<html lang="hu">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title ? title + ' | Érettségi Mentor' : 'Érettségi Mentor' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&family=Playfair+Display:wght@600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/app117/css/styles.css" />
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark gradient-bar">
    <div class="container">
      <a class="navbar-brand fw-bold" href="/app117">Érettségi Mentor</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav" aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="mainNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/app117">Főoldal</a></li>
          <li class="nav-item"><a class="nav-link" href="/app117/adatbazis">Adatbázis</a></li>
          <li class="nav-item"><a class="nav-link" href="/app117/kapcsolat">Kapcsolat</a></li>
          <% if (user) { %>
            <li class="nav-item"><a class="nav-link" href="/app117/kapcsolat/uzenetek">Üzenetek</a></li>
          <% } %>
          <% if (user && user.role === 'admin') { %>
            <li class="nav-item"><a class="nav-link" href="/app117/crud">Admin / CRUD</a></li>
          <% } %>
        </ul>
        <div class="d-flex gap-2">
          <% if (!user) { %>
            <a class="btn btn-outline-light btn-sm" href="/app117/auth/login">Bejelentkezés</a>
            <a class="btn btn-light btn-sm" href="/app117/auth/register">Regisztráció</a>
          <% } else { %>
            <div class="navbar-text text-white small me-2">Belépve: <strong><%= user.username %></strong> (<%= user.role %>)</div>
            <form action="/app117/auth/logout" method="post">
              <button class="btn btn-light btn-sm" type="submit">Kijelentkezés</button>
            </form>
          <% } %>
        </div>
      </div>
    </div>
  </nav>

  <% if (flash) { %>
    <div class="container mt-3">
      <div class="alert alert-<%= flash.type %> shadow-sm" role="alert">
        <%= flash.message %>
      </div>
    </div>
  <% } %>
